kind: Service
apiVersion: v1
metadata:
  name: model-storage-production
spec:
  selector:
    app: model-storage
    env: production
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000

---

kind: Service
apiVersion: v1
metadata:
  name: model-storage-staging
spec:
  selector:
    app: model-storage
    env: staging
  ports:
  - name: web
    protocol: TCP
    port: 80
    targetPort: 8000
